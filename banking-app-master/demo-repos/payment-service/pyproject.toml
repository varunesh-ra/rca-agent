[project]
name = "payment-service"
version = "1.0.0"
description = "Payment processing microservice"
requires-python = ">=3.11"
dependencies = [
    "stripe>=7.0.0",
    "fastapi>=0.110.0",
    "pydantic>=2.6.0",
]
